# 第一章：大模型部署

> 大模型部署是将训练好的模型应用于实际生产环境的关键步骤。本章详细介绍部署架构、硬件选择、服务化部署和容器化技术。

## 1.1 模型部署基础

### 1.1.1 部署架构设计

大模型部署面临独特挑战：模型体积大、推理计算量大、内存占用高。

**部署架构类型**：

| 架构类型 | 适用场景 | 优点 | 缺点 |
|----------|----------|------|------|
| 本地部署 | 企业内部，数据敏感 | 数据安全，延迟低 | 成本高，维护复杂 |
| 云端部署 | 通用API服务 | 弹性扩展，运维简单 | 延迟较高，费用按需 |
| 边缘部署 | 移动端、IoT设备 | 低延迟，离线可用 | 硬件受限 |
| 混合部署 | 复杂业务场景 | 兼顾安全与弹性 | 架构复杂 |

**基本部署架构**：
```
                    ┌─────────────┐
                    │   负载均衡   │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐       ┌────▼────┐
   │ GPU节点1 │       │ GPU节点2  │       │ GPU节点3 │
   └────┬────┘       └─────┬─────┘       └────┬────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    ┌──────▼──────┐
                    │  模型缓存层  │
                    │  (Redis)    │
                    └─────────────┘
```

### 1.1.2 推理框架选择

**主流推理框架对比**：

| 框架 | 开发公司 | 特点 | 适用场景 |
|------|----------|------|----------|
| vLLM | UC Berkeley | PagedAttention，高吞吐 | 大批量推理 |
| Transformers | Hugging Face | 生态完善，易用 | 通用场景 |
| TensorRT | NVIDIA | 高度优化，延迟低 | 生产部署 |
| ONNX Runtime | Microsoft | 跨平台，兼容性好 | 跨平台部署 |
| TGI | Hugging Face | 支持GGUF，量化好 | 开源模型部署 |

**vLLM部署示例**：
```python
# 使用vLLM部署模型
from vllm import LLM, SamplingParams

# 初始化
llm = LLM(model="meta-llama/Llama-2-7b-chat-hf")

# 采样参数
sampling_params = SamplingParams(
    temperature=0.7,
    max_tokens=256,
    top_p=0.95
)

# 推理
outputs = llm.generate(["请给我讲一个故事", "什么是机器学习"], sampling_params)

for output in outputs:
    print(f"Prompt: {output.prompt}")
    print(f"Generated: {output.outputs[0].text}")
```

**Hugging Face TGI部署**：
```bash
# 使用Docker启动TGI
docker run -d --gpus all \
  -p 8080:80 \
  -v /data:/data \
  ghcr.io/huggingface/text-generation-inference:latest \
  --model-id meta-llama/Llama-2-7b-chat-hf \
  --quantize fill-in-the-blank \
  --max-input-length 1024 \
  --max-total-tokens 2048
```

### 1.1.3 性能评估指标

**核心指标**：

1. **延迟（Latency）**：
   - 首Token延迟（Time to First Token，TTFT）
   - 逐Token生成延迟
   - 端到端延迟

2. **吞吐量（Throughput）**：
   - 每秒请求数（QPS）
   - 每秒Token数（Tokens/s）

3. **资源利用率**：
   - GPU利用率
   - 显存占用
   - CPU利用率

```python
class PerformanceBenchmark:
    """性能基准测试"""
    def __init__(self, model, tokenizer):
        self.model = model
        self.tokenizer = tokenizer

    def benchmark_latency(self, prompt, num_runs=100):
        """延迟测试"""
        import time

        times = []
        for _ in range(num_runs):
            start = time.time()

            # 编码
            inputs = self.tokenizer(prompt, return_tensors='pt')

            # 生成
            outputs = self.model.generate(
                **inputs,
                max_new_tokens=100,
                do_sample=False
            )

            # 解码
            response = self.tokenizer.decode(outputs[0])

            end = time.time()
            times.append(end - start)

        return {
            'mean': np.mean(times),
            'std': np.std(times),
            'p50': np.percentile(times, 50),
            'p95': np.percentile(times, 95),
            'p99': np.percentile(times, 99)
        }

    def benchmark_throughput(self, prompt, duration=60):
        """吞吐量测试"""
        import time

        start_time = time.time()
        num_requests = 0
        num_tokens = 0

        while time.time() - start_time < duration:
            inputs = self.tokenizer(prompt, return_tensors='pt')
            outputs = self.model.generate(**inputs, max_new_tokens=50)

            num_requests += 1
            num_tokens += outputs.shape[1]

        elapsed = time.time() - start_time

        return {
            'requests_per_second': num_requests / elapsed,
            'tokens_per_second': num_tokens / elapsed
        }

    def benchmark_concurrent(self, prompt, num_concurrent=10, num_requests=100):
        """并发测试"""
        import asyncio

        async def single_request():
            inputs = self.tokenizer(prompt, return_tensors='pt')
            outputs = await asyncio.create_task(
                self.model.generate_async(**inputs, max_new_tokens=50)
            )
            return outputs

        # 并发执行
        start = time.time()
        for _ in range(num_requests):
            asyncio.run(single_request())
        end = time.time()

        return {
            'total_time': end - start,
            'avg_latency': (end - start) / num_requests
        }
```

---

## 1.2 硬件部署

### 1.2.1 GPU服务器选型

**GPU选择考量**：

| GPU型号 | 显存 | 算力(TFLOPS) | 适用场景 |
|---------|------|--------------|----------|
| A100 80GB | 80GB | 312 | 大模型推理 |
| H100 80GB | 80GB | 989 | 大模型训练/推理 |
| A6000 | 48GB | 161 | 中等模型 |
| RTX 4090 | 24GB | 165 | 小模型/开发 |
| L40 | 48GB | 91 | 推理服务 |

**配置建议**：

```yaml
# 单卡推理配置（7B模型）
gpu: A100-80GB
memory: 80GB
cpus: 16
ram: 128GB

# 多卡推理配置（70B模型）
gpus: 4x A100-80GB
total_memory: 320GB
nvlink: true
cpus: 64
ram: 512GB
```

### 1.2.2 多GPU并行策略

**张量并行（Tensor Parallelism）**：

将单一层的权重分割到多个GPU上。

```python
import torch
import torch.nn as nn
from transformers import AutoModelForCausalLM

class TensorParallelModel:
    """张量并行模型"""
    def __init__(self, model_name, num_gpus=2):
        self.num_gpus = num_gpus
        self.model = self._load_tensor_parallel_model(model_name)

    def _load_tensor_parallel_model(self, model_name):
        """加载张量并行模型"""
        # 使用DeepSpeed或Megatron加载
        # 这里使用Hugging Face的accelerate库示例
        from accelerate import init_empty_weights, load_checkpoint_and_dispatch

        model = AutoModelForCausalLM.from_pretrained(
            model_name,
            device_map="auto",
            torch_dtype=torch.float16
        )
        return model

    def generate(self, prompt):
        inputs = self.tokenizer(prompt, return_tensors='pt')
        outputs = self.model.generate(**inputs)
        return self.tokenizer.decode(outputs[0])
```

**流水线并行（Pipeline Parallelism）**：

将模型的不同层分配到不同GPU上。

```python
class PipelineParallelModel:
    """流水线并行模型"""
    def __init__(self, model, num_stages=4):
        self.num_stages = num_stages
        self.stages = self._split_model(model, num_stages)

    def _split_model(self, model, num_stages):
        """将模型切分为多个阶段"""
        layers = list(model.children())
        chunk_size = len(layers) // num_stages

        stages = []
        for i in range(num_stages):
            start = i * chunk_size
            end = start + chunk_size if i < num_stages - 1 else len(layers)
            stage = nn.Sequential(*layers[start:end])
            stages.append(stage.to(f'cuda:{i}'))

        return stages

    def forward(self, x):
        """流水线前向传播"""
        for i, stage in enumerate(self.stages):
            if i == 0:
                x = x.to('cuda:0')
            x = stage(x)
            if i < self.num_stages - 1:
                x = x.to(f'cuda:{i+1}')
        return x
```

### 1.2.3 云服务部署

**AWS部署**：

```python
import boto3

def deploy_on_sagemaker():
    """在AWS SageMaker上部署模型"""
    # 创建模型
    sage_client = boto3.client('sagemaker')

    model_data_url = "s3://your-bucket/model.tar.gz"

    # 创建模型
    create_model_response = sage_client.create_model(
        ModelName="llm-model",
        PrimaryContainer={
            'Image': '763104351884.dkr.ecr.us-east-1.amazonaws.com/huggingface-pytorch-tgi:latest',
            'ModelDataUrl': model_data_url,
            'Environment': {
                'HF_MODEL_ID': 'meta-llama/Llama-2-7b-chat-hf',
                'HF_TASK': 'text-generation'
            }
        },
        ExecutionRoleArn='arn:aws:iam::123456789:role/SageMakerRole'
    )

    # 创建端点配置
    sage_client.create_endpoint_config(
        EndpointConfigName='llm-config',
        ProductionVariants=[{
            'VariantName': 'variant1',
            'ModelName': 'llm-model',
            'InstanceType': 'ml.g5.48xlarge',
            'InitialInstanceCount': 1
        }]
    )

    # 创建端点
    sage_client.create_endpoint(
        EndpointName='llm-endpoint',
        EndpointConfigName='llm-config'
    )

    return "llm-endpoint"
```

**Google Cloud Vertex AI部署**：

```python
from google.cloud import aiplatform

def deploy_on_vertex():
    """在Vertex AI上部署模型"""
    aiplatform.init(project='your-project', location='us-central1')

    # 导入模型
    model = aiplatform.Model.upload(
        display_name='llm-model',
        artifact_uri='gs://your-bucket/model',
        serving_container_image_uri='us-docker.pkg.dev/vertex-ai/prediction/tgi-gpu:latest'
    )

    # 部署
    endpoint = model.deploy(
        machine_type='a2-highgpu-4g',
        accelerator_type='NVIDIA_A100',
        accelerator_count=1
    )

    return endpoint
```

### 1.2.4 边缘设备部署

**移动端部署**：

```python
# 使用MLC-LLM进行移动端部署
import mlc_llm

# 加载模型到移动端
model = mlc_llm.quantize(
    model="Llama-2-7b-chat-hf",
    quantizer="q4f16_1",
    device="iphone"
)

# 在iOS上生成
result = model.generate(
    prompt="Hello, how are you?",
    device="iphone"
)
```

**NVIDIA Jetson边缘部署**：

```python
# 使用TensorRT进行Jetson部署
import tensorrt as trt

def build_engine(model_path, fp16=True):
    """为Jetson构建TensorRT引擎"""
    logger = trt.Logger(trt.Logger.WARNING)

    with trt.Builder(logger) as builder:
        config = builder.create_builder_config()
        config.set_memory_pool_limit(trt.MemoryPoolType.WORKSPACE, 1 << 30)  # 1GB

        if fp16:
            config.set_flag(trt.BuilderFlag.FP16)

        network = builder.create_network(1 << int(trt.NetworkDefinitionCreationFlag.EXPLICIT_BATCH))
        parser = trt.OnnxParser(network, logger)

        with open(model_path, 'rb') as f:
            parser.parse(f.read())

        engine = builder.build_serialized_network(network, config)
        return engine
```

---

## 1.3 服务化部署

### 1.3.1 API设计

**REST API设计**：

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel
from typing import Optional, List

app = FastAPI(title="LLM API", version="1.0")

class GenerateRequest(BaseModel):
    prompt: str
    max_tokens: Optional[int] = 256
    temperature: Optional[float] = 0.7
    top_p: Optional[float] = 0.9
    stop: Optional[List[str]] = None

class GenerateResponse(BaseModel):
    text: str
    finish_reason: str
    prompt_tokens: int
    completion_tokens: int

@app.post("/v1/completions", response_model=GenerateResponse)
async def generate(request: GenerateRequest):
    """文本补全接口"""
    try:
        inputs = tokenizer(request.prompt, return_tensors='pt').to(device)

        outputs = model.generate(
            **inputs,
            max_new_tokens=request.max_tokens,
            temperature=request.temperature,
            top_p=request.top_p,
            do_sample=True
        )

        response = tokenizer.decode(outputs[0], skip_special_tokens=True)

        return GenerateResponse(
            text=response,
            finish_reason="stop",
            prompt_tokens=len(inputs['input_ids'][0]),
            completion_tokens=len(outputs[0]) - len(inputs['input_ids'][0])
        )
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

@app.post("/v1/chat/completions")
async def chat_completions(messages: List[dict]):
    """Chat式接口"""
    # 将messages转换为prompt
    prompt = convert_messages_to_prompt(messages)

    inputs = tokenizer(prompt, return_tensors='pt').to(device)
    outputs = model.generate(**inputs, max_new_tokens=256)

    response = tokenizer.decode(outputs[0], skip_special_tokens=True)

    return {
        "choices": [{
            "message": {"role": "assistant", "content": response},
            "finish_reason": "stop"
        }],
        "usage": {
            "prompt_tokens": len(inputs['input_ids'][0]),
            "completion_tokens": len(outputs[0]) - len(inputs['input_ids'][0])
        }
    }
```

**OpenAI兼容接口**：

```python
# 实现OpenAI API兼容接口
class OpenAICompatibility:
    """OpenAI API兼容层"""

    def __init__(self, model):
        self.model = model
        self.tokenizer = model.tokenizer

    def completions(self, prompt, **kwargs):
        """/v1/completions 兼容"""
        inputs = self.tokenizer(prompt, return_tensors='pt')
        outputs = self.model.generate(**inputs, **kwargs)
        return {
            "choices": [{
                "text": self.tokenizer.decode(outputs[0]),
                "finish_reason": "stop"
            }]
        }

    def embeddings(self, texts):
        """/v1/embeddings 兼容"""
        embeddings = []
        for text in texts:
            inputs = self.tokenizer(text, return_tensors='pt')
            outputs = self.model(**inputs)
            embedding = outputs.last_hidden_state[:, 0].cpu().numpy()
            embeddings.append(embedding.tolist())
        return {"data": [{"embedding": e} for e in embeddings]}
```

### 1.3.2 批处理优化

**动态批处理**：

```python
class DynamicBatcher:
    """动态批处理器"""
    def __init__(self, model, max_batch_size=32, max_wait_time_ms=100):
        self.model = model
        self.max_batch_size = max_batch_size
        self.max_wait_time = max_wait_time_ms / 1000.0  # 转换为秒

        self.pending_requests = []
        self.lock = asyncio.Lock()

    async def add_request(self, prompt, params):
        """添加请求到批处理队列"""
        future = asyncio.Future()
        self.pending_requests.append({
            'prompt': prompt,
            'params': params,
            'future': future
        })

        # 检查是否可以执行
        if len(self.pending_requests) >= self.max_batch_size:
            await self._execute_batch()

        return await future

    async def _execute_batch(self):
        """执行一批请求"""
        async with self.lock:
            if len(self.pending_requests) == 0:
                return

            batch = self.pending_requests[:self.max_batch_size]
            self.pending_requests = self.pending_requests[self.max_batch_size:]

        # 准备batch输入
        prompts = [r['prompt'] for r in batch]
        params = batch[0]['params']

        # 执行推理
        outputs = self.model.generate(prompts, **params)

        # 完成futures
        for i, request in enumerate(batch):
            request['future'].set_result(outputs[i])
```

### 1.3.3 缓存策略

**KV Cache优化**：

```python
class KVCacheManager:
    """KV缓存管理器"""
    def __init__(self, max_cache_size=10000):
        self.cache = {}
        self.max_size = max_cache_size
        self.hits = 0
        self.misses = 0

    def get(self, prompt_hash):
        """获取缓存的KV"""
        if prompt_hash in self.cache:
            self.hits += 1
            return self.cache[prompt_hash]
        self.misses += 1
        return None

    def set(self, prompt_hash, kv_cache):
        """设置KV缓存"""
        if len(self.cache) >= self.max_size:
            # LRU淘汰
            oldest_key = next(iter(self.cache))
            del self.cache[oldest_key]

        self.cache[prompt_hash] = kv_cache

    def get_hit_rate(self):
        """获取缓存命中率"""
        total = self.hits + self.misses
        return self.hits / total if total > 0 else 0

class SemanticCache:
    """语义缓存 - 基于向量相似度"""
    def __init__(self, embedding_model, threshold=0.95):
        self.embedding_model = embedding_model
        self.threshold = threshold
        self.cache = []
        self.embeddings = []

    def get(self, prompt):
        """查找相似缓存"""
        query_emb = self.embedding_model.encode(prompt)

        for i, cached_emb in enumerate(self.embeddings):
            similarity = cosine_similarity(query_emb, cached_emb)
            if similarity > self.threshold:
                return self.cache[i]['response']

        return None

    def set(self, prompt, response):
        """添加缓存"""
        embedding = self.embedding_model.encode(prompt)
        self.cache.append({'prompt': prompt, 'response': response})
        self.embeddings.append(embedding)
```

### 1.3.4 负载均衡

**负载均衡策略**：

```python
import hashlib
from collections import defaultdict

class LoadBalancer:
    """请求负载均衡器"""
    def __init__(self, backends):
        """
        backends: [{"url": "http://gpu1:8000", "weight": 1}, ...]
        """
        self.backends = backends
        self.connection_counts = defaultdict(int)

    def select_backend(self, strategy='least_connections'):
        """选择后端"""
        if strategy == 'least_connections':
            # 最少连接
            min_count = min(self.connection_counts.values())
            candidates = [b for b in self.backends
                         if self.connection_counts[b['url']] == min_count]
            return candidates[0] if candidates else self.backends[0]

        elif strategy == 'round_robin':
            # 轮询
            return self.backends[0]  # 简化实现

        elif strategy == 'hash':
            # 哈希（相同prompt到相同后端）
            return self.backends[0]  # 简化实现

        return self.backends[0]

    async def forward(self, prompt, params):
        """转发请求"""
        backend = self.select_backend('least_connections')
        self.connection_counts[backend['url']] += 1

        try:
            response = await call_backend(backend['url'], prompt, params)
            return response
        finally:
            self.connection_counts[backend['url']] -= 1
```

---

## 1.4 容器化部署

### 1.4.1 Docker使用

**Dockerfile示例**：

```dockerfile
# 基础镜像
FROM nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04

# 设置环境
ENV DEBIAN_FRONTEND=noninteractive
ENV PYTHONUNBUFFERED=1

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    python3.10 \
    python3-pip \
    git \
    && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 下载模型（可选）
# RUN python -c "from huggingface_hub import snapshot_download; snapshot_download('meta-llama/Llama-2-7b')"

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["python", "server.py", "--model", "meta-llama/Llama-2-7b"]
```

**requirements.txt**：
```
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0
bitsandbytes>=0.39.0
vllm>=0.2.0
fastapi>=0.100.0
uvicorn>=0.23.0
```

### 1.4.2 Kubernetes部署

**Deployment配置**：

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: llm
  template:
    metadata:
      labels:
        app: llm
    spec:
      containers:
      - name: llm
        image: your-registry/llm-model:v1.0
        resources:
          limits:
            nvidia.com/gpu: 1
            memory: "64Gi"
            cpu: "16"
          requests:
            nvidia.com/gpu: 1
            memory: "32Gi"
            cpu: "8"
        ports:
        - containerPort: 8000
        env:
        - name: MODEL_NAME
          value: "meta-llama/Llama-2-7b"
        - name: MAX_LENGTH
          value: "2048"
        volumeMounts:
        - name: model-cache
          mountPath: /root/.cache/huggingface
      volumes:
      - name: model-cache
        persistentVolumeClaim:
          claimName: model-cache-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: llm-service
spec:
  type: ClusterIP
  selector:
    app: llm
  ports:
  - port: 80
    targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: llm-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: llm-deployment
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
```

**GPU调度配置**：

```yaml
apiVersion: v1
kind: Node
metadata:
  name: gpu-node-1
  labels:
    gpu: "true"
    nvidia.com/gpu.product: "A100-SXM4-80GB"
spec:
  taints:
  - key: "nvidia.com/gpu"
    effect: "NoSchedule"
---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: gpu-workload
value: 1000
globalDefault: false
description: "GPU workload priority"
```

### 1.4.3 服务监控与日志

**Prometheus监控指标**：

```python
from prometheus_client import Counter, Histogram, Gauge, start_http_server

# 定义指标
REQUEST_COUNT = Counter(
    'llm_requests_total',
    'Total number of requests',
    ['model', 'status']
)

REQUEST_LATENCY = Histogram(
    'llm_request_duration_seconds',
    'Request latency',
    ['model']
)

GPU_UTILIZATION = Gauge(
    'llm_gpu_utilization',
    'GPU utilization percentage',
    ['gpu_id']
)

ACTIVE_REQUESTS = Gauge(
    'llm_active_requests',
    'Number of active requests',
    []
)

def track_request_metrics(model_name):
    """请求指标跟踪装饰器"""
    def decorator(func):
        async def wrapper(*args, **kwargs):
            ACTIVE_REQUESTS.inc()
            start_time = time.time()

            try:
                result = await func(*args, **kwargs)
                REQUEST_COUNT.labels(model=model_name, status='success').inc()
                return result
            except Exception as e:
                REQUEST_COUNT.labels(model=model_name, status='error').inc()
                raise
            finally:
                duration = time.time() - start_time
                REQUEST_LATENCY.labels(model=model_name).observe(duration)
                ACTIVE_REQUESTS.dec()

        return wrapper
    return decorator
```

**日志配置**：

```python
import logging
import json
from datetime import datetime

class StructuredLogger:
    """结构化日志记录器"""
    def __init__(self, name='llm-service'):
        self.logger = logging.getLogger(name)
        self.logger.setLevel(logging.INFO)

        # 控制台输出
        console_handler = logging.StreamHandler()
        console_handler.setFormatter(logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        ))
        self.logger.addHandler(console_handler)

    def log_request(self, prompt, response, latency, metadata=None):
        """记录请求日志"""
        log_data = {
            'timestamp': datetime.utcnow().isoformat(),
            'type': 'request',
            'prompt_length': len(prompt),
            'response_length': len(response),
            'latency_ms': latency,
            **(metadata or {})
        }
        self.logger.info(json.dumps(log_data))

    def log_error(self, error, context=None):
        """记录错误日志"""
        log_data = {
            'timestamp': datetime.utcnow().isoformat(),
            'type': 'error',
            'error': str(error),
            **(context or {})
        }
        self.logger.error(json.dumps(log_data))

    def log_metrics(self, metrics):
        """记录指标日志"""
        log_data = {
            'timestamp': datetime.utcnow().isoformat(),
            'type': 'metrics',
            **metrics
        }
        self.logger.info(json.dumps(log_data))
```

---

## 本章小结

本章介绍了大模型部署的核心内容：

1. **部署架构设计**：
   - 本地、云端、边缘等多种部署模式
   - vLLM、TGI等推理框架选择
   - 延迟、吞吐量等性能指标评估

2. **硬件部署**：
   - GPU服务器选型和配置
   - 张量并行和流水线并行
   - 云服务部署（AWS、GCP）
   - 边缘设备部署（Jetson）

3. **服务化部署**：
   - REST API设计和OpenAI兼容接口
   - 动态批处理优化
   - KV Cache和语义缓存
   - 负载均衡策略

4. **容器化部署**：
   - Docker容器构建
   - Kubernetes编排和GPU调度
   - Prometheus监控和结构化日志

---

## 习题与思考

1. 大模型部署相比传统ML模型有哪些独特挑战？
2. 推理框架vLLM的PagedAttention技术有什么优势？
3. 张量并行和流水线并行各适合什么场景？
4. 如何设计一个高效的动态批处理系统？
5. 在Kubernetes中如何保证GPU调度的高效性？

## 参考资料

1. "vLLM: Easy, Fast, and Cheap LLM Serving with PagedAttention" - Kwon et al.
2. "Hugging Face Text Generation Inference" - 官方文档
3. "Deploying LLMs on Kubernetes" - cloud-native best practices
4. "Optimizing Inference for Large Language Models" - NVIDIA documentation